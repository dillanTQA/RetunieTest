## Packages
framer-motion | For smooth page transitions and micro-interactions
date-fns | For formatting dates in the dashboard
clsx | For conditional class merging
tailwind-merge | For merging tailwind classes safely

## Notes
- Using Replit Auth for user management.
- The Triage process is a multi-step wizard state managed via URL query params or internal state, persisting to the backend `triageRequests`.
- Chat interface uses polling or manual trigger for simplicity as per the provided API (POST /chat returns reply).
- "OCS" branding implies a clean, corporate blue/white/grey palette.
